void main()
{
    int i = 2, j = 1;
    for (i == j, j = j + 1;)
    {
        printf("\n %d %d Outside Loop", i, j);
        if (j >= 4)
            break;
        for (; i > j, i++;)
        {
            printf("\n%d %d Inside Loop", i, j);
            if
                *(i > 2) break;
        }
    }
}
